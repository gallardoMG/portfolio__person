import ReactDOM from 'react-dom';
import './styles/index.css';
import React from 'react';
import App from './App';

ReactDOM.render(
  <App />,
  document.getElementById('root')
);

// instalar sass npm install -g node-sass 
// para compilar node-sass -w carpeta de origen -o carpeta nueva donde se guardara (node-sass -w src -o portfolio)
